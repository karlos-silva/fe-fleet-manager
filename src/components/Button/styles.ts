import styled from 'styled-components';

export const ContainerButton = styled.button`
  cursor: pointer;
  padding: 10px 15px;
  display: flex;
  align-items: center;

  svg {
    margin-left: 5px;
  }
`